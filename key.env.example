# Copy to .env or key.env and fill in your values. Do not commit .env or key.env.
# The code loads .env first, then key.env (so .env overrides).

# GCP project ID (required for Vertex AI)
GOOGLE_CLOUD_PROJECT=your-project-id
# Or: PROJECT_ID=your-project-id

# API key (e.g. from Vertex express mode; full Cloud API access). Loaded from this file.
GOOGLE_API_KEY=your-api-key
# Or: API_KEY=your-api-key

# If using a service account instead of API key (optional):
# GOOGLE_APPLICATION_CREDENTIALS=path/to/your-service-account-key.json

# Vertex AI region (optional, default: us-central1)
VERTEX_LOCATION=us-central1

# Copy to key.env and fill in your values. Do not commit key.env.

# GCP project ID (required for Vertex AI)
GOOGLE_CLOUD_PROJECT=your-project-id
# Or use PROJECT_ID=your-project-id

# Service account JSON path (if not using gcloud application-default login)
# GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account.json

# Vertex AI region (optional, default: us-central1)
VERTEX_LOCATION=us-central1
